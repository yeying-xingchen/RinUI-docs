# Get QQmlApplicationEngine

## Introduction

`engine` is a attribute in the RinUIWindow class.If you need use the QQmlApplicationEngine() from QtQuick,you need the attribute.

## Usage

```py
main = RinUIWindow("main.qml")
engine = main.engine
```
